The subdirectories contain short examples demonstrating various aspects of
Sprite.  To run an example, cd to its directory and execute the "run" script.

Before running these, you must say "make stage" from the root of this project.
This installs sprite under ../install.
